targets:
  $default:
    builders:
      # The id a surface names when it is created against this catalog.
      # Reverse-domain notation, as A2UI recommends.
      genui_gen_builder:genui_catalog:
        options:
          catalog_id: dev.dlsoft.genui_gen.example
